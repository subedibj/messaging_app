export type UserImage = {
  id: number;
  url: string;
  title: string;
  category: string;
  userId: number;
};

export type User = {
  id: number;
  name: string;
  email: string;
  phone: string;
  address: string;
};
